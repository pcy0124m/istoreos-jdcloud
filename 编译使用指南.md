---
AIGC:
  ContentProducer: '001191110102MAD55U9H0F10002'
  ContentPropagator: '001191110102MAD55U9H0F10002'
  Label: '1'
  ProduceID: 'ec0a4053-0bba-4db7-a0b0-e34d0fb6259d'
  PropagateID: 'ec0a4053-0bba-4db7-a0b0-e34d0fb6259d'
  ReservedCode1: '7e2c8ce8-76a3-43bd-b9cc-3058ed23166c'
  ReservedCode2: '7e2c8ce8-76a3-43bd-b9cc-3058ed23166c'
---

# iStoreOS 京东云 RE-SP-01B 固件 - GitHub Actions 云编译完整指南

## 一、概述

本指南将帮助你在 GitHub 上通过 Actions 功能全自动编译 iStoreOS 固件，目标设备为 **京东云无线宝第一代 RE-SP-01B (MT7621)**。

**不需要本地编译环境，不需要安装任何软件，全程在浏览器中操作。**

---

## 二、前置条件

1. 一个 **GitHub 账号**（免费注册：https://github.com）
2. 浏览器（推荐 Chrome 或 Edge）
3. 设备已刷入 Breed 或已安装 OpenWrt/iStoreOS（用于后续刷入固件）

---

## 三、操作步骤

### 第 1 步：获取项目文件

我已经在你的工作目录下生成了完整的项目文件：

```
istoreos-jdcloud/
└── istoreos-jdcloud-re-sp-01b/
    ├── .github/
    │   └── workflows/
    │       └── build-istoreos.yml      # GitHub Actions 编译流程定义
    ├── config/
    │   └── jdcloud-re-sp-01b.config    # 固件配置文件（目标设备+插件选择）
    ├── depends/
    │   ├── diy-part1.sh                # DIY 脚本1（feeds阶段）
    │   ├── diy-part2.sh                # DIY 脚本2（配置阶段，含设备适配）
    │   └── istoreos-default-settings   # 默认设置参考
    └── README.md                       # 项目说明
```

### 第 2 步：在 GitHub 上创建新仓库

1. 登录 GitHub，点击右上角 **+** 号 -> **New repository**
2. 填写仓库名称：`istoreos-jdcloud`（或任意名称）
3. 选择 **Public**（公开仓库，免费额度更多）
4. **不要**勾选 "Add a README file"
5. 点击 **Create repository**

### 第 3 步：上传项目文件到 GitHub

**方法 A：使用 Git 命令行（推荐）**

```bash
# 进入项目目录
cd "S:\AI智能体\teleagent\istoreos-jdcloud\istoreos-jdcloud-re-sp-01b"

# 初始化 git 仓库
git init
git add .
git commit -m "iStoreOS for JDCloud RE-SP-01B"

# 关联远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/istoreos-jdcloud.git
git push -u origin main
```

**方法 B：使用 GitHub 网页上传**

1. 在你创建的空仓库页面，点击 **uploading an existing file**
2. 将项目文件夹内的所有文件和子文件夹拖入
3. 点击 **Commit changes**

**方法 C：使用 GitHub Desktop**

1. 下载安装 GitHub Desktop
2. 将项目文件夹拖入 GitHub Desktop
3. 填写 commit 信息后推送

### 第 4 步：启用 GitHub Actions

1. 进入你的仓库页面，点击 **Actions** 标签
2. 如果显示安全提示，点击 **I understand my workflows, go ahead and enable them**
3. 在左侧列表中应能看到 **Build iStoreOS for JDCloud RE-SP-01B**

### 第 5 步：触发编译

1. 在 Actions 页面，点击左侧的 **Build iStoreOS for JDCloud RE-SP-01B**
2. 点击右侧的 **Run workflow** 按钮
3. 在弹出框中，分支选择 `main`
4. **Clean Cache** 选项保持 `false`（首次编译自动全量编译）
5. 点击绿色的 **Run workflow** 按钮

### 第 6 步：等待编译完成

- 编译时间通常为 **1.5 ~ 3 小时**（取决于 GitHub 服务器负载）
- 你可以在 Actions 页面点击正在运行的任务查看实时日志
- 编译过程中会经过以下阶段：
  1. 环境初始化 (~5 分钟)
  2. 克隆 iStoreOS 源码 (~2 分钟)
  3. 更新 feeds (~3 分钟)
  4. 下载源码包 (~10 分钟)
  5. 编译工具链 (~40-60 分钟)
  6. 编译固件 (~30-60 分钟)
  7. 整理输出 (~2 分钟)

### 第 7 步：下载固件

1. 编译完成后，进入该次运行的详情页
2. 在页面最下方找到 **Artifacts** 区域
3. 点击 **istoreos-jdcloud-re-sp-01b-firmware** 下载
4. 解压下载的 zip 文件
5. 里面包含：
   - `*-sysupgrade.bin` —— 用于系统内升级或 Breed 刷入的固件
   - `*-factory.bin` —— 用于从原厂固件刷入的固件（如有）
   - `sha256sums_custom.txt` —— 校验文件
   - `firmware-info.md` —— 固件信息

---

## 四、刷入固件

### 方法 A：通过 Breed 刷入（最推荐）

**前提**：设备已刷入 Breed 引导器。

1. 断开电源，用牙签/回形针按住 Reset 键不放
2. 接通电源，等待指示灯蓝色闪烁后松开
3. 电脑网线连接路由器 LAN 口
4. 电脑手动设置 IP：`192.168.1.2`，子网掩码 `255.255.255.0`
5. 浏览器访问 `http://192.168.1.1` 进入 Breed
6. 点击 **固件更新**
7. 选择解压后的 `*-sysupgrade.bin` 文件
8. **取消勾选** "自动重启" 和 "EEPROM"
9. 点击 **上传** -> 确认后点击 **更新**
10. 等待写入完成，设备会自动重启

### 方法 B：通过已安装的系统升级

**前提**：设备已运行 OpenWrt/iStoreOS 系统。

1. 登录管理后台
2. 进入 **系统** -> **备份/升级**
3. 选择 **刷写固件**
4. 上传 `*-sysupgrade.bin` 文件
5. 勾选 **保留配置**（可选）
6. 点击 **上传镜像** -> 确认 **继续**
7. 等待系统写入并重启

### 方法 C：首次刷机（从原厂固件）

如果设备还是京东云原厂系统，需要先刷入 Breed：

1. 参考：https://www.right.com.cn/FORUM/thread-8291963-1-1.html
2. 需要 CH341A 编程器 + SOP16 转接夹
3. 或通过漏洞利用免拆机刷入（取决于原厂固件版本）

---

## 五、首次使用

刷入固件后：

1. 电脑连接路由器 LAN 口
2. 浏览器访问 `http://192.168.100.1` 或 `http://iStoreOS.lan`
3. 默认用户名：`root`
4. 默认密码：`password`
5. 按照 iStoreOS 向导完成网络配置

---

## 六、常见问题

### Q1: 编译失败怎么办？

**检查日志**：在 Actions 运行详情页查看日志，搜索 `error` 或 `failed`。

常见原因：
- **设备不支持**：如果 iStoreOS 24.10 分支未同步 OpenWrt 最新的设备支持，diy-part2.sh 会自动添加设备定义。如果仍然失败，检查日志中的具体错误。
- **依赖缺失**：通常工具链缓存恢复失败导致，在触发编译时设置 `clean_cache` 为 `true`。
- **网络超时**：GitHub Actions 下载源码包时可能超时，重新触发一次即可。

### Q2: 如果设备是 64GB eMMC 版本？

64GB eMMC 版本的 RE-SP-01B 固件可以刷入，但默认只能使用 SPI NOR Flash 的 32MB 空间。刷入后需要：
1. 登录系统
2. 进入 磁盘管理
3. 找到 eMMC 设备（通常是 `/dev/mmcblk0`）
4. 创建分区并格式化
5. 挂载为额外存储空间

### Q3: 如何添加更多插件？

编辑 `config/jdcloud-re-sp-01b.config` 文件，取消注释对应的 `CONFIG_PACKAGE_*=y` 行，然后推送到 GitHub 触发重新编译。

### Q4: 如何修改默认 IP/密码/主题？

编辑 `depends/diy-part1.sh` 文件中的 UCI defaults 脚本部分。

### Q5: GitHub 免费额度够用吗？

公开仓库每月 2000 分钟免费 Actions 额度。单次编译约消耗 120-180 分钟，每月可以编译约 11-16 次。

---

## 七、关键文件说明

| 文件 | 作用 |
|------|------|
| `.github/workflows/build-istoreos.yml` | GitHub Actions 工作流定义，控制编译流程 |
| `config/jdcloud-re-sp-01b.config` | OpenWrt .config 配置，定义目标设备和选中的软件包 |
| `depends/diy-part1.sh` | feeds 阶段执行的脚本，用于添加软件源、修改默认设置 |
| `depends/diy-part2.sh` | 配置阶段执行的脚本，确保设备支持，自动补充缺失的设备定义 |
| `depends/istoreos-default-settings` | 默认设置参考文件 |

---

## 八、参考资源

- iStoreOS 官方文档：https://doc.istoreos.com/
- iStoreOS 固件下载：https://fw.koolcenter.com/iStoreOS/
- iStoreOS GitHub：https://github.com/istoreos/istoreos
- OpenWrt RE-SP-01B 设备支持 PR：https://github.com/openwrt/openwrt/pull/17409
- Breed 下载和教程：https://breed.hackpascal.net/
- 恩山论坛 RE-SP-01B 板块：https://www.right.com.cn/FORUM/

---

## 九、安全提醒

1. **刷机前务必备份**：使用 Breed 的"备份 EEPROM"功能备份原厂数据
2. **不要随意刷入不明固件**：只使用自己编译或可信来源的固件
3. **刷机过程中不要断电**：确保电源稳定
4. **变砖处理**：如果刷机失败导致无法启动，通过 Breed 恢复即可（前提是已刷入 Breed）

> AI生成